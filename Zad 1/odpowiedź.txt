Proszę odpowiedzieć na pytanie za pomocą jakiego mechanizmu aplikacja wydrukowała w konsoli komunikat ‘new connection’?
Aplikacja wydrukowała w konsoli komunikat 'new connection' za pomocą metody console.log() wtedy gdy zostanie wywołane zdarzenie 'connection' na serwerze HTTP.